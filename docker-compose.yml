name: promark-portal

services:
  auth:
    build:
      context: ./server/auth-service
      dockerfile: Dockerfile
    container_name: auth-service
    restart: unless-stopped
    env_file: ./.env.production
    expose:
      - "5001"
    networks:
      - promark-portal
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:5001/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1))"]
      interval: 30s
      timeout: 10s
      start_period: 5s
      retries: 3

  notifications:
    build:
      context: ./server/notification-service
      dockerfile: Dockerfile
    container_name: notification-service
    restart: unless-stopped
    env_file: ./.env.production
    expose:
      - "5002"
    networks:
      - promark-portal
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:5002/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1))"]
      interval: 30s
      timeout: 10s
      start_period: 5s
      retries: 3

  projects:
    build:
      context: ./server/project-numbering-service
      dockerfile: Dockerfile
    container_name: project-numbering-service
    restart: unless-stopped
    env_file: ./.env.production
    expose:
      - "5003"
    networks:
      - promark-portal
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:5003/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1))"]
      interval: 30s
      timeout: 10s
      start_period: 5s
      retries: 3

  callid:
    build:
      context: ./server/callid-service
      dockerfile: Dockerfile
    container_name: callid-service
    restart: unless-stopped
    env_file: ./.env.production
    expose:
      - "5004"
    networks:
      - promark-portal
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:5004/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1))"]
      interval: 30s
      timeout: 10s
      start_period: 5s
      retries: 3

  auth-gateway:
    build:
      context: ./server/auth-gateway
      dockerfile: Dockerfile
    container_name: auth-gateway
    restart: unless-stopped
    env_file: ./.env.production
    expose:
      - "5005"
    networks:
      - promark-portal
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:5005/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1))"]
      interval: 30s
      timeout: 10s
      start_period: 5s
      retries: 3

  sample-automation:
    build:
      context: ./server/sample-automation-service
      dockerfile: Dockerfile
    container_name: sample-automation-service
    restart: unless-stopped
    env_file: ./.env.production
    expose:
      - "5006"
    networks:
      - promark-portal
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:5006/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1))"]
      interval: 30s
      timeout: 10s
      start_period: 5s
      retries: 3

  user-management:
    build:
      context: ./server/user-management-service
      dockerfile: Dockerfile
    container_name: user-management-service
    restart: unless-stopped
    env_file: ./.env.production
    expose:
      - "5007"
    networks:
      - promark-portal
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:5007/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1))"]
      interval: 30s
      timeout: 10s
      start_period: 5s
      retries: 3

  quota-management:
    build:
      context: ./server/quota-management-service
      dockerfile: Dockerfile
    container_name: quota-management-service
    restart: unless-stopped
    env_file: ./.env.production
    expose:
      - "5008"
    networks:
      - promark-portal
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:5008/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1))"]
      interval: 30s
      timeout: 10s
      start_period: 5s
      retries: 3

  reporting:
    build:
      context: ./server/reporting-service
      dockerfile: Dockerfile
    container_name: reporting-service
    restart: unless-stopped
    env_file: ./.env.production
    expose:
      - "5009"
    networks:
      - promark-portal
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:5009/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1))"]
      interval: 30s
      timeout: 10s
      start_period: 5s
      retries: 3

  project-info:
    build:
      context: ./server/project-info-service
      dockerfile: Dockerfile
    container_name: project-info-service
    restart: unless-stopped
    env_file: ./.env.production
    expose:
      - "5010"
    networks:
      - promark-portal
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:5010/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1))"]
      interval: 30s
      timeout: 10s
      start_period: 5s
      retries: 3

  project-publishing:
    build:
      context: ./server/project-publishing-service
      dockerfile: Dockerfile
    container_name: project-publishing-service
    restart: unless-stopped
    env_file: ./.env.production
    expose:
      - "5011"
    networks:
      - promark-portal
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:5011/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1))"]
      interval: 30s
      timeout: 10s
      start_period: 5s
      retries: 3

  disposition:
    build:
      context: ./server/disposition-service
      dockerfile: Dockerfile
    container_name: disposition-service
    restart: unless-stopped
    env_file: ./.env.production
    expose:
      - "5012"
    networks:
      - promark-portal
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:5012/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1))"]
      interval: 30s
      timeout: 10s
      start_period: 5s
      retries: 3

  ai-prompting:
    build:
      context: ./server/ai-prompting-service
      dockerfile: Dockerfile
    container_name: ai-prompting-service
    restart: unless-stopped
    env_file: ./.env.production
    expose:
      - "5013"
    networks:
      - promark-portal
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:5013/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1))"]
      interval: 30s
      timeout: 10s
      start_period: 5s
      retries: 3

  github:
    build:
      context: ./server/github-service
      dockerfile: Dockerfile
    container_name: github-service
    restart: unless-stopped
    env_file: ./.env.production
    expose:
      - "5014"
    networks:
      - promark-portal
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:5014/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1))"]
      interval: 30s
      timeout: 10s
      start_period: 5s
      retries: 3

  promark-employees:
    build:
      context: ./server/promark-employees-service
      dockerfile: Dockerfile
    container_name: promark-employees-service
    restart: unless-stopped
    env_file: ./.env.production
    expose:
      - "5015"
    networks:
      - promark-portal
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:5015/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1))"]
      interval: 30s
      timeout: 10s
      start_period: 5s
      retries: 3

  api:
    build:
      context: ./server
      target: production
    restart: unless-stopped
    env_file: ./.env.production
    volumes:
      - ./server_logs:/logs
    networks:
      - promark-portal

  client:
    build: 
      context: ./client
      target: production
      args:
        VITE_DEV_API_URL: ${VITE_DEV_API_URL}
        VITE_DOMAIN_NAME: ${VITE_DOMAIN_NAME}
        VITE_ENV: ${VITE_ENV}
    restart: unless-stopped
    env_file: ./.env.production
    networks:
      - promark-portal
    depends_on:
      - api

  caddy:
    build:
      context: ./caddy
      dockerfile: Dockerfile
      args:
        ENVIRONMENT: production
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    env_file: ./.env.production
    environment:
      VITE_DOMAIN_NAME: ${VITE_DOMAIN_NAME}
    volumes:
      # - ./caddy/Caddyfile.production:/etc/caddy/Caddyfile
      - caddy_data:/caddy_data
      - caddy_config:/caddy_config
      - /var/log/caddy_logs:/var/log/caddy
    networks:
      - promark-portal
    depends_on:
      - auth
      - auth-gateway
      - notifications
      - projects
      - callid
      - sample-automation
      - user-management
      - quota-management
      - reporting
      - project-info
      - project-publishing
      - disposition
      - ai-prompting
      - github
      - promark-employees
      - api
      - client

volumes:
  caddy_data:
  caddy_config:

networks:
  promark-portal:
    driver: bridge