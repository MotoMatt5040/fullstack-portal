name: Deploy Sample Automation Service

on:
  push:
    branches:
      - main
      - testing
    paths:
      - 'server/sample-automation-service/**'
  workflow_dispatch:

jobs:
  deploy-testing:
    if: github.ref == 'refs/heads/testing'
    runs-on: ubuntu-latest
    environment: testing

    steps:
      - name: Deploy Sample Automation Service to Testing
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.TESTING_SSH_HOST }}
          username: ${{ secrets.TESTING_SSH_USERNAME }}
          key: ${{ secrets.TESTING_SSH_PRIVATE_KEY }}
          script: |
            echo "🚀 Deploying sample-automation-service to TESTING at $(date)"

            DEPLOY_PATH="$HOME/fullstack_portal"
            cd $DEPLOY_PATH

            echo "📥 Pulling latest code..."
            git pull origin testing

            echo "🔨 Rebuilding sample-automation service..."
            docker compose --env-file .env.testing -f docker-compose.testing.yml up -d --build --no-deps sample-automation

            echo "⏳ Waiting for service to start..."
            sleep 10

            echo "🧪 Health Check:"
            echo -n "  Sample Automation Service: " && docker exec sample-automation-service node -e "require('http').get('http://localhost:5006/health', (r) => { console.log(r.statusCode === 200 ? '✅ OK' : '❌ Not ready'); process.exit(0); })" || echo "❌ Not ready"

            echo "✅ Sample automation service deployment to TESTING completed at $(date)"

  deploy-production:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Deploy Sample Automation Service to Production
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.PRODUCTION_SSH_HOST }}
          username: ${{ secrets.PRODUCTION_SSH_USERNAME }}
          key: ${{ secrets.PRODUCTION_SSH_PRIVATE_KEY }}
          script: |
            echo "🚀 Deploying sample-automation-service to PRODUCTION at $(date)"

            DEPLOY_PATH="$HOME/fullstack_portal"
            cd $DEPLOY_PATH

            echo "📥 Pulling latest code..."
            git pull origin main

            echo "🔨 Rebuilding sample-automation service..."
            docker compose --env-file .env.production up -d --build --no-deps sample-automation

            echo "⏳ Waiting for service to start..."
            sleep 10

            echo "🧪 Health Check:"
            echo -n "  Sample Automation Service: " && docker exec sample-automation-service node -e "require('http').get('http://localhost:5006/health', (r) => { console.log(r.statusCode === 200 ? '✅ OK' : '❌ Not ready'); process.exit(0); })" || echo "❌ Not ready"

            echo "✅ Sample automation service deployment to PRODUCTION completed at $(date)"
